image: xileon/goparty:latest

stages:
  - test

task-test:
  stage: test
  script:
    - checkout
    - docker run $CI_REGISTRY_IMAGE:latest task test
  # Run this job in a branch where a Dockerfile exists
  rules:
    - if: $CI_COMMIT_BRANCH
      exists:
        - Dockerfile

